### Gov-Vendor-Public-IP

The public IP address of the servers the <a href="/connection-method/#what-is-your-originating-device">originating device</a> sent their requests to.

* This could be an IP address of a Web Application Firewall (WAF), a DDoS Protection Service, or a load balancer that the vendor's DNS record resolves to.

* If the connection between client and server is over a private network, you will not be able to collect a public IP address. You need to <a href="/getting-it-right/#missing-header-data">check what to do about missing data</a>.

<strong>Example</strong>

<pre><code>Gov-Vendor-Public-IP: 203.0.113.6</code></pre>
