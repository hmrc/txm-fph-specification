### Gov-Client-Local-IPs

A list of all local IPv4 and IPv6 addresses available to the <a href="../#originating-device">originating device</a>.

<% if locals[:connection_method] == 'WEB_APP_VIA_SERVER' %>
* In some cases, it will be difficult to get these values but your application must try to obtain them

* If you obtain a multicast DNS name instead of an IP address, you must still submit it
<% end %>

* Every IPv6 value in the list must be <a href="https://tools.ietf.org/html/rfc3986#section-2.1" target="_blank" rel="noopener noreferrer">percent encoded (opens in a new tab)</a>. Do not percent encode separators (commas).

<% if locals[:connection_method] == 'WEB_APP_VIA_SERVER' %>
* You may be able to use WebRTC to discover local device IPs
<% end %>

<strong>Examples</strong>

<pre><code>Gov-Client-Local-IPs: fc00%3A%3A,10.1.2.3</code></pre>

<pre><code>Gov-Client-Local-IPs: 10.1.2.3,10.3.4.2</code></pre>

<pre><code>Gov-Client-Local-IPs: 65f46603-1f39-4ca5-957f-65e231804f91.local</code></pre>
